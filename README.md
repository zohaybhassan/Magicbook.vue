# Magicbook

A Vue and Vite online book library with authentication, catalog, and admin views.

## Local development

Enter the `test` directory, run `npm install`, then start the app with `npm run dev`.

To serve the bundled `db.json` on port 3000, run `node server.js` in a second terminal.
